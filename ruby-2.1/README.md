1uptalent/ruby-2.1
==================

Ubuntu based image with ruby 2.1 compiled from source.

Check the `Dockerfile` for the exact versions.

## Running

This will get you into a nice `irb` shell with history and completion.

    docker run -i -t 1uptalent/ruby-2.1 irb
