1uptalent/haproxy-1.5
=====================

Ubuntu based image with haproxy-1.5 compiled from source.

Check the `Dockerfile` for the exact versions.

## Running

This will get you into a `bash` shell.

    docker run -i -t 1uptalent/haproxy-1.5 bash
